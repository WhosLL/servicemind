node_modules/
.next/
.vercel
*.log

# Environment variables — NEVER commit. Real values live in Vercel for prod,
# and in .env.local for local dev (pull via `vercel env pull`).
.env
.env.*
!.env.example

.env*.local
